# 回顾性补测 v11：候选多样性修订

日期：2026-09-19。v11已启动；实时状态为 `results/pae_public_pilot_v11/pipeline_status.json`。

## v10实际结果

v10的768次生成全部完成，PUB001仅有9条非原生唯一序列，未达到预设的每组至少10条门槛。128次生成中TTLTT出现55次、TTLTI出现48次，两条合计103次（80.5%）；原生序列计数为0。其余5组唯一非原生候选数分别为118、44、73、88、46。

v10按协议在教师评分前停止，未产生AF2标签或最终效果指标。这是候选多样性不足，不是WSL崩溃，也不是已证明Ridge筛选失败。停止记录、原0.1温度、候选和门槛均保留。

## v11修订与不变项

在新目录、新协议中将所有6组ProteinMPNN温度统一设为0.3；种子固定为20260919、20261919、20262919、20263919，每种子32次，总共仍为768次。调整旨在提高采样多样性，是否达到门槛以实际生成结果为准。温度和种子同时变化，因此不把它称为仅隔离温度因素的因果消融实验。

v10候选池不混入v11。继续要求每组至少10条唯一非原生序列，按事先固定的哈希顺序最多取20条；未达标仍在新标签前停止。没有降低门槛、挑选PAE较好的骨架或对新标签调参。

冻结Ridge参数、设计PDB特征、单生成器范围、6组/4类抗原分层、前20%筛选预算、随机基线、AF2模型1/2×3种子×3recycle、96任务分批和失败处理均保持v10规则。完整科学方案见 `results/pae_public_pilot_v11/protocol.json`，其中记录了父协议与父候选库存的哈希以及修订原因。

仍属于回顾性描述研究，已知抗原重叠和预训练不确定性没有因本次采样修订而消失。本轮全部候选的AF2计算用于获得评价真值，不代表此运行本身节省了80%计算。

## 执行与验证

入口为 `scripts/run_pae_public_pilot_v11.py`；从v10实现另存并冻结，v10实现不改。后台流水线依次生成、检查库存、固定设计输入预测、运行AF2、保存最终评估。执行日志为 `pipeline.stdout.log`、`pipeline.stderr.log`。

已核对执行文件哈希、Ridge/教师规则不变、新旧种子不重叠、选择顺序确定性、指标计算及v10没有教师结果。相关5项测试通过。此处“已启动”不表示已经完成生成或取得正向结果；实际进度以状态文件为准。
