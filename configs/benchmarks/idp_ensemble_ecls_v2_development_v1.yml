schema_version: 1
status: v2_mechanism_frozen_before_jackknife_heldout_effect_access
classification: exposed_12_component_development_not_confirmation

inputs:
  development:
    path: reviewer_outputs/idp_ensemble_ecls_scoring_dev_v1/analysis.json
    sha256: ea40c8e07ed7985a56c13f6a00626485791305effeb2c7749c78fcb840413acf
  expansion:
    path: reviewer_outputs/idp_ensemble_ecls_scoring_expansion_v1/analysis.json
    sha256: ce1ea4ffb61801562ed94a928a46bf5001e65b9c6551a529b12bc99df670916e

methods: [proteinmpnn, single_conformer_bfn, esm_if]
primary_method: proteinmpnn
selector:
  name: jackknife_lower_envelope
  outer_evaluation: leave_one_conformer_out
  inner_score: minimum_robust_score_across_each_additional_training_conformer_deletion
  candidate_tie_break: lexicographically_greatest_sequence
  coverage: 1.0
  robust_aggregation:
    p25_weight: 0.45
    minimum_weight: 0.25
    mean_weight: 0.20
    standard_deviation_penalty: 0.10
  single_state_comparator: unchanged_v1_median_heldout_score_of_training_conformer_winners

report_only_abstention:
  selector_identity: v2_winner_equals_all_training_single_state_winners
  minimum_median_training_candidate_range: 1.0e-4
  abstentions_are_missing_not_zero: true

statistics:
  inference_unit: independent_antibody_reference_component
  bootstrap_trials: 10000
  bootstrap_seed: 8291
  exact_paired_sign_flip: true
  mathematical_effect_epsilon: 1.0e-9

development_gates_for_future_freeze:
  minimum_valid_components: 12
  minimum_strictly_positive_components: 8
  require_component_bootstrap_ci95_lower_above_zero: true
  maximum_negative_components: 1

output:
  json: reviewer_outputs/idp_ensemble_ecls_v2_development_v1/analysis.json
  report: reviewer_outputs/idp_ensemble_ecls_v2_development_v1/report.md

claim_boundary: v2 mechanism development on 12 exposed computational components; any passing result only licenses freezing the selector for a new untouched panel
