schema_version: 1
status: post_iptm_invariance_development_diagnostic
classification: retrospective_development_not_confirmation

input:
  path: reviewer_outputs/idp_ensemble_bfn_fixed_scoring_dev_v1/analysis.json
  sha256: bb278fefeb7c82092f3900d80b02ea406f027cf0ae72ca9f04e291475bcd6fa7

endpoint:
  name: bfn_fixed_candidate_state_compatibility
  direction: higher_is_better
  reason: candidate_sensitive_output_after_context_only_iptm_invariance
  selection_protocol: leave_one_conformer_out
  ensemble_aggregation:
    p25_weight: 0.45
    minimum_weight: 0.25
    mean_weight: 0.20
    standard_deviation_penalty: 0.10
  single_state_comparator: median_heldout_score_of_each_training_conformer_winner

statistics:
  inference_unit: independent_antibody_reference_component
  bootstrap_trials: 10000
  bootstrap_seed: 8157
  exact_paired_sign_flip: true

development_gates:
  minimum_valid_components_per_generator: 6
  minimum_positive_component_fraction: 0.6666666666666666
  require_component_bootstrap_ci95_lower_above_zero: true

output: reviewer_outputs/idp_ensemble_bfn_state_compatibility_dev_v1/analysis.json
claim_boundary: >-
  Post-failure development diagnostic using already frozen fixed candidate
  scores. A passing result may only nominate state_compatibility as a frozen
  endpoint for a new untouched panel; it cannot rescue or replace the failed
  ipTM endpoint and cannot establish binding or IDP design success.
